# Multi-Modal Physiological Monitoring System – Dependencies

# ===== CORE VISION & SIGNAL PROCESSING =====
opencv-python>=4.8.0          # Video capture, face ROI extraction, image ops
mediapipe>=0.10.0             # Face detection (FaceDetection) and 468-point FaceMesh
numpy>=1.24.0                 # Numerical arrays and FFT
scipy>=1.10.0                 # Butterworth filters, Welch PSD, detrend

# ===== DEEP LEARNING =====
torch>=2.0.0                  # PyTorch – LSTM model, tensor ops
torchvision>=0.15.0           # Vision utilities (transforms, etc.)

# ===== MACHINE LEARNING =====
scikit-learn>=1.3.0           # LogisticRegression, RandomForest
joblib>=1.3.0                 # Model serialisation / deserialisation

# ===== WEB API =====
flask>=3.0.0                  # REST API server
flask-cors>=4.0.0             # Cross-Origin Resource Sharing for frontend

# ===== UTILITIES =====
python-dotenv>=1.0.0          # .env file support (WEBCAM_INDEX, FLASK_PORT, etc.)

# ===== OPTIONAL EMOTION DETECTION BACKENDS =====
# Install ONE of the following for production-quality emotion detection.
# The system auto-detects which is available and falls back gracefully.
#
# Option A – DeepFace (downloads pre-trained weights automatically):
#   pip install deepface
#
# Option B – FER (Fast & Accurate, lightweight, no download required):
#   pip install fer
#
# Without either, the built-in geometry heuristic is used automatically.
